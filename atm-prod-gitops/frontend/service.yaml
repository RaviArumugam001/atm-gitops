apiVersion: v1
kind: Service
metadata:
  name: atm-fe-svc
  namespace: atm-prod
spec:
  type: NodePort
  selector:
    app: atm-fe
  ports:
  - name: http
    port: 80
    targetPort: 80
    nodePort: 30082
