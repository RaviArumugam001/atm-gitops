apiVersion: apps/v1
kind: Deployment
metadata:
  name: ecommerce-backend
  namespace: ecommerce-prod
spec:
  replicas: 3
  selector:
    matchLabels:
      app: ecommerce-backend
  template:
    metadata:
      labels:
        app: ecommerce-backend
    spec:
      imagePullSecrets:
        - name: docker-registry-creds
      containers:
      - name: flask
        image: raviarumugam001/ecommerce-backend:latest
        ports:
          - containerPort: 5000
        envFrom:
          - configMapRef:
              name: ecommerce-config
          - secretRef:
              name: ecommerce-secrets
